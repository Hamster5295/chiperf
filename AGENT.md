# chiperf

我希望创建 chiperf, 一种用于硬件微架构性能分析与可视化的通用文件格式, 以及其解析器、可视化器套件。

在当前阶段，我希望制定 chiperf 文件格式规范。


## 使用场景

chiperf 预期在 RTL 仿真阶段生成, 期望 RTL 工程师通过插桩, 在 RTL 的重要阶段以 "写文件" 的方式记录各个事件  

这些事件按照一套共同的规范写入文件, 即 chiperf 文件, 从而允许后续使用同一套解析器进行解析、可视化等工作


## 基本要求

1. chiperf 应当是 **文本** 格式, 后缀为 `.chiperf`
2. chiperf 允许使用 gzip 压缩, 后缀为 `.chiperf.gz`
3. chiperf 应当 **事件驱动**
4. chiperf 应当具备 **鲁棒性**, 即: 若仿真器因故中断, 或其他原因导致 chiperf 文件不完整, 则解析器应当能较为简单地处理完整、不受影响的部分
5. chiperf 应当 **人类友好**, 即: 人类阅读其内容, 应当至少能理解其描述的大概过程


## 追踪能力

1. chiperf 需要能追踪时钟, 包括上升沿与下降沿
2. chiperf 需要能追踪特定事件的次数, 以及他们发生时的对应时钟沿
2. chiperf 需要能追踪特定数值的变化, 以及他们发生时的对应时钟沿
3. chiperf 需要能追踪特定模块内部正在处理的内容, 通过模块的输入和输出事件实现 (如 CPU 流水线特定流水级正在执行的指令, 可以通过第 N 周期输入 -> 第 N+M 周期输出 来追踪)
4. chiperf 需要能追踪特定模块内部的状态机变化

在上述能力中, 每一项应当对应至少一个 **事件**; 每一项都必须有一个名字 (比如, 模块名、变量名或事件名), 方便可视化


## 期望效果

以下是一个期望的文件示例, 仅作为参考: 
```chiperf
[clk] p
[cnt] "Branch Miss"
[cnt] "Cache Hit"
[val] "PC", 0x800001d0
[pip] "IF", I, 0x1234abcd
[pip] "IF", O
[clk] n
[clk] p
......
```

## 要求

1. 仅制定协议, 不写代码
2. 在 docs 里写 Markdown 文档